class Solution {
public:
    void reverseString(vector<char>& s) {
        
        reverse(s.begin(),s.end());
        
    }
};

Or you can also :

st=0,e=s.size()-1;
while(st<e){
  swap(s[st],s[e]);
  st++;
  e--;
  }
